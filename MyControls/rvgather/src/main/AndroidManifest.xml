<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="zs.xmx.rvgather">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GatherRecycleViewActivity" />
        <activity android:name=".RecyclerViewActivity" />
        <activity android:name=".ChatRvActivity" />
        <activity android:name=".GridViewTitleActivity" />
        <activity android:name=".RadioSelectItemActivity" />
        <activity android:name=".CheckItemActivity" />
        <activity android:name=".CheckAndSelectItemActivity" />
    </application>

</manifest>