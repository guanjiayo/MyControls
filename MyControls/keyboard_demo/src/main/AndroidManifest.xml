<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="zs.xmx">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 静态设置输入模式 -->
        <!-- android:windowSoftInputMode="adjustPan" -->
        <!-- android:windowSoftInputMode="adjustResize" -->
        <activity
            android:name=".AdjustResize_KeyboardActivity"
            android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".Scroll_keyboardActivity"/>
        <activity
            android:name=".AdjustPan_KeyboardActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity
            android:name=".Allup_KeyboardActivity"
            android:windowSoftInputMode="adjustResize">
        </activity>
        <activity
            android:name=".Complex_KeyboardActivity"
            android:windowSoftInputMode="adjustResize">
        </activity>
        <activity
            android:name=".FullscreenFail_Activity"
            android:windowSoftInputMode="adjustResize">
        </activity>
        <activity android:name=".StatusBarFail_KeyboardActivity"
            >
        </activity>
    </application>

</manifest>